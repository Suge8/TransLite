# Optional. Used in provider HTTP headers and docs links.
# WXT_WEBSITE_URL=https://translite.app

# Keep this true only when intentionally skipping build-time env validation.
# WXT_SKIP_ENV_VALIDATION=true
